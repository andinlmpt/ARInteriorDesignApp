// Shared types for theme recommendation components
export interface BudgetOption {
  value: 'low' | 'medium' | 'high' | 'luxury';
  label: string;
  emoji: string;
  range: string;
  description: string;
}


